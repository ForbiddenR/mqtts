# Build assets

This directory contains Wails build resources such as the app icon and platform-specific packaging assets.

`appicon.png` is a placeholder generated during Phase 2 bootstrap and should be replaced with production artwork before release.
